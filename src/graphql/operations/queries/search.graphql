query Search(
    $query: String!,
    $endCursor: String,
    $startCursor: String,
    $limit: Int = 10
) {
    search(type: USER, query: $query, first: $limit, after: $endCursor, before: $startCursor){
        ...searchResultFields
    }
}
